<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.insurance_system.insurance.repository.ContractMapper">

    <!-- 계약 여부 확인 -->
    <select id="checkCustomerInsuranceContract" parameterType="map" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM contract
        WHERE customer_id = #{customerId} AND insurance_id = #{insuranceId}
    </select>

    <!-- 계약 정보 조회 -->
    <select id="getContractByCustomerAndInsurance" parameterType="map" resultType="com.example.insurance_system.insurance.entity.Contract">
        SELECT * FROM contract
        WHERE customer_id = #{customerId} AND insurance_id = #{insuranceId}
    </select>

    <!-- 계약 저장 -->
    <insert id="insertContract" parameterType="com.example.insurance_system.insurance.entity.Contract">
        INSERT INTO contract (customer_id, insurance_id, contract_date, amount, status)
        VALUES (#{customerId}, #{insuranceId}, #{contractDate}, #{amount}, #{status})
    </insert>

    <!-- 계약 정보 업데이트 -->
    <update id="updateContract" parameterType="com.example.insurance_system.insurance.entity.Contract">
        UPDATE contract
        SET contract_date = #{contractDate}, amount = #{amount}, status = #{status}
        WHERE customer_id = #{customerId} AND insurance_id = #{insuranceId}
    </update>

    <!-- 계약 삭제 -->
    <delete id="deleteContract" parameterType="map">
        DELETE FROM contract
        WHERE customer_id = #{customerId} AND insurance_id = #{insuranceId}
    </delete>

</mapper>
